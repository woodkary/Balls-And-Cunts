server:
  port: 9000

spring:
  application:
    name: service-monitor
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        ephemeral: false

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: always