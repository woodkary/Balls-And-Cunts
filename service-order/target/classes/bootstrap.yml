server:
  port: 8000

spring:
  boot:
    admin:
      client:
        url: http://localhost:9000
  application:
    name: service-order
  cloud:
    nacos:
      server-addr: localhost:8848
      discovery:
        ephemeral: false